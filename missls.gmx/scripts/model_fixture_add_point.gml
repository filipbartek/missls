///model_fixture_add_point(fixture, point)

assert(is_descendant(obj_model));

var fixture = argument0;
var point = argument1;

physics_fixture_add_point(fixture, point[0] * scale, point[1] * scale);
